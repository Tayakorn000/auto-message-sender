# AutoSender v1.6.5 — ส่วนขยายอัปเดตเองอัตโนมัติ

## 🆕 1.6.5 — ไม่ต้องโหลดไฟล์เองอีกแล้ว

เปิดโปรแกรม = มันเช็คให้เอง เจอตัวใหม่ **ทับโฟลเดอร์ส่วนขยายให้ทันที ไม่ต้องกดอะไรเลย**
เหลือแค่ปิด Chrome เปิดใหม่ (ซึ่งขั้นตอน "เปิด Chrome Profiles" ก็ต้องปิดอยู่แล้ว)

ตัวโปรแกรม (.exe) ยังต้องกดปุ่ม 1 ที เพราะ Windows เขียนทับไฟล์ที่กำลังรันอยู่ไม่ได้
แต่กดแล้วมันโหลดเอง สลับไฟล์เอง เปิดกลับเอง — ไม่ต้องเข้าเว็บไปโหลดเองอีกต่อไป

หาโฟลเดอร์ส่วนขยายไม่เจอจะเงียบไว้ ไม่เด้งถามตอนเปิดโปรแกรม (ไปถามตอนกดปุ่มอัปเดตแทน)
ทับไม่สำเร็จถึงจะขึ้นเตือน


## 🆕 1.6.4 — แก้ปุ่มที่ Facebook แทรกช่องว่างพิเศษ + บอกให้รู้ว่าส่วนขยายเก่า

**อาการ**: กด "เริ่มต้นใช้งาน" ไม่ไป และแชร์ไม่ไป

**เจอ 2 อย่าง**

1. Facebook แทรกช่องว่างชนิดพิเศษ (NBSP) คั่นคำในป้ายปุ่ม โค้ดเทียบชื่อปุ่มแบบตรงตัวเลยพลาด
   ตอนแก้ปุ่ม "เริ่มต้นใช้งาน" รอบก่อนล้างค่านี้ไว้แล้ว แต่โค้ดไลค์/แชร์ที่อยู่ไฟล์เดียวกันยังไม่ได้ล้าง
   ตอนนี้ใช้ตัวล้างเดียวกันทั้งไฟล์ + เพิ่มเทสเคส NBSP
   ของแถม: ป้าย "Remove Like" ที่มี NBSP เคยถูกอ่านเป็น "ยังไม่ได้ไลค์" แล้วกดซ้ำ = **ไลค์หลุด** ตอนนี้ไม่หลุดแล้ว

2. **โปรแกรมเคยเงียบสนิทเมื่อส่วนขยายเก่ามาก** — ตัวก่อน 1.6.0 ไม่ได้ส่งเวอร์ชันตัวเองมาให้
   โปรแกรมเลยไม่มีอะไรจะเทียบ ไม่ขึ้นเตือนเลย ทั้งที่นี่คือเคสที่ต้องจับที่สุด
   (เอา .exe ใหม่ไปวาง แต่โฟลเดอร์ `extension` ยังเป็นของเก่า = ปุ่มมีในโปรแกรม แต่กดแล้วไม่มีอะไรเกิด)
   ตอนนี้ poll ที่ไม่บอกเวอร์ชันถูกนับเป็น "เก่ามาก" และขึ้นเตือนพร้อมชื่อ preset

**เวอร์ชันขึ้นที่หัวหน้าต่างแล้ว** — `Auto Messenger v1.6.4 | ส่วนขยาย 1.6.4`
ถ่ายรูปหน้าจอส่งมารูปเดียว รู้เลยว่าตัวโปรแกรมหรือส่วนขยายตัวไหนตกรุ่น


## 🆕 1.6.2 — ส่วนขยายอัปเดตให้เองแล้ว ไม่ต้องกด Reload

กดปุ่มอัปเดตในโปรแกรม มันจะเขียนทับโฟลเดอร์ `extension/` ให้ก่อน แล้วค่อยเปลี่ยนตัวโปรแกรม
จากนั้น **ปิด Chrome ให้หมดแล้วเปิดใหม่** ทุกโปรไฟล์ได้ตัวใหม่พร้อมกัน

**หลาย profile ทำไงต่อ** — ไม่ต้องทำอะไรเพิ่ม ทุกโปรไฟล์ที่ Load unpacked จากโฟลเดอร์เดียวกัน
อ่านไฟล์ชุดเดียวกันบนดิสก์ ทับทีเดียวได้ครบ (Chrome อ่านไฟล์ unpacked ใหม่ทุกครั้งที่เปิดโปรแกรม
ที่ต้องกด Reload คือกรณีแก้ไฟล์ตอน Chrome ยังเปิดค้างอยู่)

ถ้าเคยก๊อปโฟลเดอร์ส่วนขยายแยกไว้คนละที่ต่อโปรไฟล์ อันที่ไม่ได้ถูกทับจะขึ้นเตือนในแท็บ
🌐 Setup Chrome พร้อมชื่อ preset ว่าตัวไหนตกรุ่น — เอาโฟลเดอร์เดียวกันให้ทุกโปรไฟล์จะง่ายกว่า

โปรแกรมหาโฟลเดอร์จาก `extension/` ที่วางข้าง ๆ ตัวโปรแกรม (หรือ path ที่จำไว้ใน
`autosender_config.json`) ตอนแตกไฟล์จะไม่ยอมรับซิปที่เขียนออกนอกโฟลเดอร์หรือไม่มี `manifest.json`

## ⚠️ อ่านก่อน: ต้องเปลี่ยน 2 อย่าง ไม่ใช่อย่างเดียว

1. **`main.exe`** — ตัวใหม่แล้ว (build 1.6.3) มีปุ่ม 👍 ไลค์ / 🔁 แชร์ หัวหน้าต่างจะขึ้น `v1.6.3`
   ถ้าหัวหน้าต่างยังเขียนแค่ "Auto Messenger" เฉย ๆ = ยังเปิดตัวเก่าอยู่
2. **ส่วนขยาย Chrome** — รอบนี้ต้องเอาโฟลเดอร์ `extension/` ไปวางทับเองครั้งเดียว
   แล้วกด **Reload ทุกโปรไฟล์** ที่ `chrome://extensions` (ตัวเก่าไม่มีระบบอัปเดต)
   เช็คว่าใต้ชื่อส่วนขยายขึ้น **1.6.3** ถ้าขึ้น 1.0 = ยังเป็นตัวเก่า ตัวแก้ยังไม่ทำงาน
   ครั้งต่อ ๆ ไปโปรแกรมทับให้เอง แค่ปิด Chrome แล้วเปิดใหม่

อาการ "ส่งเริ่มต้นไม่ไป" อยู่ในส่วนขยาย ไม่ใช่ใน `.exe` — เปลี่ยน `.exe` อย่างเดียวไม่หาย
ตัวใหม่จะโชว์เวอร์ชันส่วนขยายในแท็บ **🌐 Setup Chrome** ให้เห็นเลยว่าโปรไฟล์ไหนลืม Reload

### แก้เพิ่มใน 1.6.1

- **ไม่ต้องติ๊ก "นี่คือลิงก์หน้าเพจ" ก็กดปุ่ม "เริ่มต้นใช้งาน" ให้แล้ว** — เดิมการรอปุ่มผูกกับ
  ช่องติ๊กนั้น ลืมติ๊กทีเดียว = ปุ่มไม่ถูกกด ส่งไม่ออกทั้งงาน ตอนนี้รอให้เสมอ (สูงสุด 500 ms
  ครั้งเดียวต่อหน้า ส่งครั้งถัด ๆ ไปไม่รอซ้ำ วัดได้ 0 ms)
  ช่องติ๊กยังต้องใช้อยู่สำหรับปุ่ม "ส่งข้อความ" ของหน้าเพจ
- **ปุ่ม "เริ่มต้นใช้งาน" หาเจอกว้างขึ้น** — เดิมดูแค่ `div[role="button"]` ที่ข้อความตรงเป๊ะ
  ตอนนี้ดู `span`/`a`/`button` ด้วย เทียบ `aria-label` ด้วย และล้าง NBSP/ช่องว่างซ้อนก่อนเทียบ
  (Facebook ชอบใส่ข้อความซ้อนใน span ลูก แบบเดิมจะพลาด) — เทียบกับโค้ดไลค์/แชร์ในไฟล์เดียวกัน
  ตัวเดิมเงื่อนไขแคบกว่าอยู่แล้ว

หมายเหตุสำหรับคนที่จะ build เอง: build แบบ `--windowed` ทำให้ `sys.stdout/stderr` เป็น `None`
พอ werkzeug พิมพ์ banner ตอนสตาร์ท thread จะตายเงียบ ๆ = API port 5000 ไม่ขึ้นเลย ส่วนขยายไม่เห็นงาน
`main.py` กันไว้แล้ว (ไม่ใช่บั๊กที่มีในตัวที่ใช้อยู่ เจอตอน build ตัวนี้) และ `--windowed`
แปลว่าไม่มีหน้าต่างดำ console โผล่มาด้วย

## 🆕 อัปเดตอัตโนมัติ

เปิดโปรแกรมแล้วเช็คเวอร์ชันให้เอง (ยิงขอแค่ชื่อเวอร์ชัน ไม่โหลดไฟล์ = ไม่กินเน็ต)
เจอตัวใหม่ค่อยขึ้นปุ่มให้กด ไม่กดก็ไม่โหลด อยู่ในแท็บ **🌐 Setup Chrome**

- ไม่มีตัวใหม่ / เน็ตไม่ดี = ขึ้น "ใช้เวอร์ชันล่าสุดอยู่แล้ว" เงียบ ๆ ไม่มี error เด้ง
- กดอัปเดต = โหลดไฟล์ใหม่ไว้ข้าง ๆ แล้วปิดโปรแกรม ให้สคริปต์สลับไฟล์แล้วเปิดกลับให้เอง
  (Windows เขียนทับ .exe ที่รันอยู่ไม่ได้)
- ส่วนขยายถูกทับให้พร้อมกัน แล้วบอกเวอร์ชันตัวเองกลับมา **โปรไฟล์ไหนตกรุ่นจะขึ้นเตือนพร้อมชื่อ preset**

รีโปที่ใช้อัปเดต: **https://github.com/Tayakorn000/auto-message-sender** (public แล้ว โหลดได้ไม่ต้องมี token)

🔴 ต้องออก Release ใหม่ทุกครั้งที่จะให้ลูกค้าได้ตัวใหม่ — แก้ `APP_VERSION` ใน `main.py`
กับ `version` ใน `extension/manifest.json` ให้ตรงกัน แล้วออก Release ที่ tag ตรงกัน
(เช่น `V1.7.0`) แนบ **2 ไฟล์**: `.exe` และ `extension.zip` (ซิปไฟล์ในโฟลเดอร์ extension แบบแบน)

Release ล่าสุดตอนนี้คือ `V1.6.3` — ทดสอบแล้วว่าตัวเวอร์ชันเก่ามองเห็น โหลด extension.zip
มาทับโฟลเดอร์ได้จริง (manifest เปลี่ยนเป็น 1.6.3 ครบ 5 ไฟล์) และปฏิเสธซิปที่เวอร์ชันไม่ตรง tag

---

# แก้บั๊กคลิกจอ + เพิ่มโหมดไลค์/แชร์

## 🆕 โหมดใหม่: 👍 กดไลค์โพสต์ / 🔁 แชร์โพสต์

ใช้เหมือนส่งข้อความทุกอย่าง: เลือกโหมด → วาง URL โพสต์ → กด "สั่งเปิดหน้าเว็บ" → กดปุ่มสั่งงาน
(โหมดนี้ไม่มีช่องข้อความกับจำนวนครั้ง เพราะไม่ต้องพิมพ์อะไร ทำครั้งเดียวจบ)

- **ไลค์** — กดปุ่มถูกใจของโพสต์ แล้วเช็คว่าปุ่มเปลี่ยนสถานะจริง
  โพสต์ที่ไลค์ไว้แล้ว **ไม่กดซ้ำ** (กดซ้ำ = เอาไลค์ออก)
- **แชร์** — กดแชร์ → "แชร์ตอนนี้" หรือ "แชร์ไปยังฟีด" → ถ้าเปิดกล่องเขียนโพสต์ กดปุ่มโพสต์ให้อีกที
- ทั้งคู่จำกัดขอบเขตอยู่ในตัวโพสต์ (`role="article"` ชั้นนอกสุด) ไม่ไปโดนปุ่มถูกใจของเพจด้านบน
  และไม่โดนปุ่มถูกใจของคอมเมนต์ด้านล่าง — เทียบชื่อปุ่มแบบตรงตัว ไม่ไปโดน "แชร์แล้ว 12 ครั้ง"
- ไลค์/แชร์ ยิงเฉพาะแท็บโพสต์ ไม่ยิงแท็บแชท

⚠️ Facebook จะจำกัดบัญชีที่ไลค์/แชร์รัวเกินไป ควรเว้นช่วงเอง

---

# แก้ปัญหา "ต้องเอาเมาส์ไปคลิกก่อน ถึงจะส่งข้อความได้"

## สาเหตุจริง

ในวิดีโอเห็นชัด: แท็บ Messenger **เป็นแท็บที่เลือกอยู่ หน้าต่างเห็นเต็มจอ หน้าโหลดเสร็จแล้ว**
แต่ยังไม่ส่ง จนกระทั่งเอาเมาส์ไปคลิกในกล่องพิมพ์แล้วส่งทันที

ตัวการอยู่ที่โค้ดหากล่องพิมพ์ใน `content.js`:

- รู้จักกล่องพิมพ์จาก `aria-label` ที่เขียนตายตัวไว้เท่านั้น
  (`"message"`, `"ข้อความ"`, `"ส่งข้อความ"`, `"สนทนา"`, `"chat"`)
- มองแค่ `div[role="textbox"]` แบบเดียว

พอ Facebook ใช้คำอื่นหรือโครงสร้างอื่น ตัวกรองหาไม่เจอ เหลือทางเดียวคือ
`document.activeElement` ซึ่งจะมีค่าก็ต่อเมื่อ **คนคลิกในกล่องเอง** —
คนกำลังทำหน้าที่หากล่องแทนโค้ด นั่นคือเหตุผลที่พอเอาเมาส์ไปคลิกแล้วส่งได้ทันที

ทดสอบแล้วว่า `document.execCommand('insertText')` พิมพ์ได้ปกติโดยไม่ต้องมีคนคลิกเลย
(`probe_focus.py`) เพราะฉะนั้นไม่ใช่เรื่องโฟกัสหรือ user gesture

## แก้ (ตัวหลัก) — `extension/content.js`

- อ่าน `aria-placeholder` / `placeholder` / `data-text` เพิ่มจาก `aria-label`
- รับทุก `[contenteditable="true"]` ไม่ใช่แค่ `div[role="textbox"]`
- เพิ่มคำที่รับ (`เขียนข้อความ`, `พิมพ์ข้อความ`, `Aa`, `ตอบกลับ` ฯลฯ)
- ทางสุดท้าย: เอากล่องที่อยู่ล่างสุดของจอ = ช่องพิมพ์ของหน้าแชท
  แทนการคืนค่าว่างแล้ววนรอจนคนไปคลิก
- กันไม่ให้ไปโดนกล่องคอมเมนต์ในโหมด Messenger (เพราะตอนนี้ค้นกว้างขึ้น)
- รอปุ่ม "เริ่มต้นใช้งาน" อีกสูงสุด 500 ms หลังเจอกล่องพิมพ์
  โค้ดหากล่องตัวใหม่เจอกล่องเร็วกว่าที่ Facebook จะ render ปุ่มเสร็จ ไม่รอ = ปุ่มไม่ถูกกด
  แล้วพิมพ์ลงกล่องที่ยังใช้งานไม่ได้ ส่งไม่ออก (โค้ดเดิมหากล่องไม่เจอ เลยวน retry
  จนปุ่มโผล่พอดี — ของใหม่เร็วเกินจนหลุด)
  1.6.1: เลิกผูกกับช่องติ๊ก "นี่คือลิงก์หน้าเพจ" แล้ว รอให้ทุกกรณี เสีย 500 ms ครั้งเดียว
  ต่อการโหลดหน้า ส่งครั้งถัด ๆ ไปวัดได้ 0 ms

## แก้ (ตัวเสริม) — คนละปัญหา

แท็บที่ถูกซ่อน (ไม่ใช่แท็บที่เลือกอยู่ในหน้าต่างนั้น) โดน Chrome หน่วง `setTimeout`
เหลือราว 1 ครั้ง/วินาที ลูป retry ของส่วนขยายเลยช้ามาก — **ไม่ใช่อาการที่เจอในวิดีโอ**
แต่เป็นปัญหาจริงตอนเปิดหลายแท็บพร้อมกัน

วัดจริงบน Windows (ลูป 20 รอบ ห่างรอบละ 10 ms ควรจบราว 200 ms):

| สภาพแท็บ | แฟล็กปิดการหน่วง | เวลา |
|---|---|---|
| ถูกซ่อน | ไม่ใส่ | **18,996 ms** |
| ถูกซ่อน | ใส่ | 199 ms |
| เป็นแท็บที่เลือกอยู่ (หน้าต่างไม่มีโฟกัส) | ไม่ใส่ | 200 ms |

- `extension/background.js` สั่งเลือกแท็บเป้าหมายก่อนส่งคำสั่งทุกครั้ง
- `main.py` / `main.exe` เปิด Chrome พร้อมแฟล็กปิดการหน่วง

## วิธีใช้

1. **โหลดส่วนขยายใหม่ทุกโปรไฟล์** — `chrome://extensions` แล้วกด Reload
   หรือ Developer mode > Load unpacked > เลือกโฟลเดอร์ `extension/`
   (ข้อนี้สำคัญที่สุด ตัวแก้หลักอยู่ในส่วนขยาย)
2. ปิด Chrome ให้หมดก่อนกดปุ่ม "🚀 เปิด Chrome Profiles" ใน `main.exe`
   (ถ้า Chrome เปิดค้างอยู่ Windows จะส่งคำสั่งไปที่ตัวที่รันอยู่แล้ว แฟล็กจะไม่ถูกใช้)
3. ใช้งานตามปกติ ไม่ต้องเอาเมาส์ไปคลิกก่อนแล้ว

## ไฟล์

- `extension/` — ส่วนขยาย Chrome ที่แก้แล้ว (**ตัวแก้หลัก**)
- `main.exe` — build ใหม่ 1.6.3 (ไลค์/แชร์ + อัปเดตทั้งโปรแกรมและส่วนขยาย) ทดสอบแล้วว่าเปิด API port 5000 จริง
- `main.py` — ซอร์สฝั่งเซิร์ฟเวอร์
- `test_find_input.py` — ดึงโค้ดหากล่องตัวจริงจาก `content.js` มารันใน headless Chrome
  กับ DOM 6 แบบ โดยไม่มีการคลิกเลย โค้ดเดิมล้ม 4/6 โค้ดใหม่ผ่าน 6/6 (`python3 test_find_input.py`)
- `test_get_started.py` — เช็คว่ากดปุ่ม "เริ่มต้นใช้งาน" ก่อนพิมพ์เสมอ และไม่ค้างเมื่อไม่มีปุ่ม
  (`python3 test_get_started.py`)
- `test_like_share.py` — เช็คไลค์/แชร์กับหน้าจำลอง 9 แบบ ตรวจว่ากดปุ่มไหนบ้าง
  (`python3 test_like_share.py`)
- `test_gui_modes.py` — เช็คว่าสลับโหมดแล้ว UI ถูก และคำสั่งส่งออกถูก (`python3 test_gui_modes.py`)
- `test_update.py` — เช็คระบบอัปเดตกับเซิร์ฟเวอร์จำลอง (เทียบเวอร์ชัน, เน็ตล่ม, ไฟล์ไม่ครบ,
  สคริปต์สลับไฟล์, เตือนส่วนขยายเก่า) (`python3 test_update.py`)
- `test_background.js` — เช็คว่าสั่งเลือกแท็บก่อนส่งคำสั่งเสมอ (`node test_background.js`)
- `probe_focus.py` — พิสูจน์ว่า `execCommand` ไม่ต้องรอ user gesture
- `probe_hidden_tab.py` — วัดการหน่วง timer ของแท็บที่ถูกซ่อน (รันบน Windows)

## หมายเหตุ

การวางข้อความด้วย synthetic `ClipboardEvent('paste')` ใน `content.js` ไม่ทำงานเลย
ทุกกรณีที่วัด ที่ใช้ได้จริงคือ `document.execCommand('insertText')` บรรทัดถัดไป
ตอนนี้ผลลัพธ์ถูกอยู่แล้วเพราะมีบรรทัดนั้นรองรับ แต่ paste คือโค้ดตายที่ตัดทิ้งได้
